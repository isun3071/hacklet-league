id: perf-dom-001
bundle: performance
category: dom-size
pool: public
evidence_model: oracle
penalty: 7   # PROVISIONAL (re-priced on v19). Excessive DOM (slows style/layout/interaction). Lighthouse
             # dom-size-insight node count. needs=1400 (LH warn) half, fail=2500 full.
applicability:
  requires: [lighthouse]
probe:
  predicate: lighthouse_audit
  audit: dom-size-insight
  mode: numeric
  needs_above: 1400
  fail_above: 2500
