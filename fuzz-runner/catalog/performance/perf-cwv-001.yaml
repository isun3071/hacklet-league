id: perf-cwv-001
bundle: performance
category: web-vitals
variant_group_id: perf-cwv   # same "slow user-facing render" flaw as perf-cwv-002 (full CWV); fire once
pool: public
evidence_model: oracle
penalty: 8   # slow First Contentful Paint — the user-facing "your vibe-coded app is slow" signal
applicability:
  requires: [browser]
probe:
  predicate: slow_first_paint
  target: /slow
  threshold_ms: 1000
