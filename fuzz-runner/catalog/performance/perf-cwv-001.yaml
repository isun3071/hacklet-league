id: perf-cwv-001
bundle: performance
category: web-vitals
pool: public
evidence_model: oracle
penalty: 22   # PROVISIONAL (re-priced on v19). Slow First Contentful Paint. Lighthouse metric, MEDIAN-of-3
              # (timing swings run-to-run), tiered on Lighthouse's own good/needs-improvement/poor band.
applicability:
  requires: [lighthouse]
probe:
  predicate: lighthouse_audit
  audit: first-contentful-paint
  mode: score
