id: perf-cache-001
bundle: performance
category: caching
pool: public
evidence_model: oracle
penalty: 16   # PROVISIONAL (re-priced on v19). Inefficient cache policy. Lighthouse cache-insight correctly
              # treats content-hashed immutable bundles as cached (our probe flagged them ~62% FP).
applicability:
  requires: [lighthouse]
probe:
  predicate: lighthouse_audit
  audit: cache-insight
  mode: score
