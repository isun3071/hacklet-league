id: perf-loadtime-001
bundle: performance
category: load-time
pool: public
evidence_model: oracle
penalty: 34   # PROVISIONAL (re-priced on v19). Slow visual load (Speed Index = the whole visual-fill curve,
              # not one milestone). Lighthouse metric, MEDIAN-of-3.
applicability:
  requires: [lighthouse]
probe:
  predicate: lighthouse_audit
  audit: speed-index
  mode: score
