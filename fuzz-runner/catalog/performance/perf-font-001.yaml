id: perf-font-001
bundle: performance
category: font-loading
pool: public
evidence_model: oracle
penalty: 6   # PROVISIONAL (re-priced on v19). Blocking font-display (FOIT). Lighthouse font-display-insight
             # excludes next/font local() fallbacks + handles encoded display=swap (our probe got both wrong).
applicability:
  requires: [lighthouse]
probe:
  predicate: lighthouse_audit
  audit: font-display-insight
  mode: score
