id: perf-requests-001
bundle: performance
category: request-count
pool: public
evidence_model: provable
penalty: 4    # >50 round trips to render the homepage (chatty) on the published profile
applicability:
  requires: [at_least_one_http_endpoint_exists]
probe:
  predicate: perf_request_count
  target: /
