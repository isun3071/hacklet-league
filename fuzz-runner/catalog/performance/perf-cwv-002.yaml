id: perf-cwv-002
bundle: performance
category: web-vitals
pool: public
evidence_model: oracle
penalty: 28   # PROVISIONAL (re-priced on v19). Core Web Vitals: LCP / TBT / CLS -- the WORST band drives the
              # tier. Lighthouse metrics, MEDIAN-of-3.
applicability:
  requires: [lighthouse]
probe:
  predicate: lighthouse_audit
  audits: [largest-contentful-paint, total-blocking-time, cumulative-layout-shift]
  mode: score
