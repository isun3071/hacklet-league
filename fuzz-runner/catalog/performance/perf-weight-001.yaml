id: perf-weight-001
bundle: performance
category: page-weight
pool: public
evidence_model: oracle
penalty: 22   # PROVISIONAL (re-priced on v19). Heavy page (transfer bytes). Lighthouse total-byte-weight.
              # needs=2MB half, fail=4MB full -- tiering collapses the old profile+ceiling probes into one.
applicability:
  requires: [lighthouse]
probe:
  predicate: lighthouse_audit
  audit: total-byte-weight
  mode: numeric
  needs_above: 2000000
  fail_above: 4000000
