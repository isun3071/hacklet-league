id: perf-load-001
bundle: performance
category: load-resilience
pool: public
evidence_model: oracle
penalty: 10   # falls over (5xx) under a modest concurrent burst -> resource-exhaustion / no locking
applicability:
  requires: [at_least_one_http_endpoint_exists]
probe:
  predicate: load_resilience
  target: /report
