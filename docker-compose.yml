# HackLet League — portable deployment stack.
#
# Host-agnostic: runs identically on the home Proxmox VM today and a Hetzner VPS
# later. Only .env differs between hosts. See DEPLOY.md.
#
#   caddy    -> serves the landing page at /, reverse-proxies /api /admin /static to backend
#   backend  -> Django (gunicorn). Runs migrate + collectstatic on start.
#   db       -> PostgreSQL 16

services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    # Host networking so Caddy sees the REAL client IP. On a bridge network, Docker's
    # NAT masquerades traffic destined to the VM's own Tailscale IP as a 172.x address,
    # which defeats the tailnet admin gate. On the host network Caddy binds :80/:443
    # directly and reaches the app over the loopback-published ports below.
    # NOTE: keep Docker's userland-proxy at its default (true) — setting it false breaks
    # those 127.0.0.1-published upstreams.
    network_mode: host
    environment:
      SITE_ADDRESS: ${SITE_ADDRESS:?set SITE_ADDRESS in .env (e.g. hackletleague.com for HTTPS, or http://localhost)}
      LAN_ADDRESS: ${LAN_ADDRESS:-http://localhost}
      ACME_EMAIL: ${ACME_EMAIL:?set ACME_EMAIL in .env (for Let's Encrypt)}
      # Secret admin slug — Caddy serves it only to the tailnet. Must match the backend's.
      ADMIN_PATH: ${ADMIN_PATH:-admin}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./landing:/srv:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend
      - frontend

  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      DJANGO_SETTINGS_MODULE: ${DJANGO_SETTINGS_MODULE:-hacklet.settings.dev}
      SECRET_KEY: ${SECRET_KEY:?set SECRET_KEY in .env}
      DATABASE_URL: ${DATABASE_URL:-postgres://hacklet:hacklet@db:5432/hacklet}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-*}
      CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS:-https://hackletleague.com}
      # Secret path the Django admin is mounted at. Must match Caddy's ADMIN_PATH.
      ADMIN_PATH: ${ADMIN_PATH:-admin}
      # Transactional email. RESEND_API_KEY (HTTP API) wins; SMTP is the fallback.
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      EMAIL_HOST: ${EMAIL_HOST:-}
      EMAIL_PORT: ${EMAIL_PORT:-587}
      EMAIL_HOST_USER: ${EMAIL_HOST_USER:-}
      EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD:-}
      EMAIL_USE_TLS: ${EMAIL_USE_TLS:-true}
      DEFAULT_FROM_EMAIL: ${DEFAULT_FROM_EMAIL:-HackLet League <no-reply@hackletleague.com>}
      # Newsletter (Buttondown) — secret API key for the server-side signup proxy.
      BUTTONDOWN_API_KEY: ${BUTTONDOWN_API_KEY:-}
      # Google OAuth (social login); read by settings.SOCIALACCOUNT_PROVIDERS.
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_SECRET: ${GOOGLE_OAUTH_SECRET:-}
    depends_on:
      db:
        condition: service_healthy
    # Published on loopback only so the host-networked Caddy can reach it (Caddy can't
    # use the docker-network name `backend` while on the host net). Not exposed off-box.
    ports:
      - "127.0.0.1:8000:8000"
    # Private submission archives (MEDIA_ROOT=/app/media). Persisted across deploys; never
    # served publicly — downloads go through the auth-gated API endpoint.
    volumes:
      - submissions_media:/app/media

  frontend:
    build: ./frontend
    restart: unless-stopped
    environment:
      # Server-side (SSR) fetches reach the backend over the internal docker network.
      INTERNAL_API_URL: http://backend:8000
    # Published on loopback only so the host-networked Caddy can reach it. The frontend
    # stays on the default bridge, so its SSR fetches to `backend` still resolve.
    ports:
      - "127.0.0.1:3000:3000"

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-hacklet}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-hacklet}
      POSTGRES_DB: ${POSTGRES_DB:-hacklet}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-hacklet}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  submissions_media:
  caddy_data:
  caddy_config:
  pgdata:
